"""
Integration tests for TrackX.

These tests require actual dependencies (ultralytics, paddleocr) to be installed
and may require network access for model downloads. They are separated from the
unit tests in tests/ to avoid sys.modules pollution and to allow them to be
skipped when dependencies are unavailable.
"""
